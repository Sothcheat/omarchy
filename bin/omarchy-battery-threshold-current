#!/bin/bash

# Script to get current battery charge threshold
# Returns: 80 or 100

STATE_FILE="$HOME/.local/state/omarchy/battery-threshold"

# If state file exists, return its value
if [[ -f "$STATE_FILE" ]]; then
  cat "$STATE_FILE"
else
  # Default to 80% for battery health
  echo "80"
fi
